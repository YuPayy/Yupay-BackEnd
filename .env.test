# Test environment
PORT=3001
NODE_ENV=test

# Test Database (separate from dev)
DATABASE_URL="mysql://root:@127.0.0.1:3306/yupay_test_db"

# JWT
JWT_SECRET=test-jwt-secret-key

# Google OAuth (placeholder for test)
GOOGLE_CLIENT_ID=test-placeholder
GOOGLE_CLIENT_SECRET=test-placeholder

# OCR
PYTHON_OCR_URL=http://localhost:5057/ocr

# Email (not used in tests — mocked)
EMAIL_USER=test@test.com
EMAIL_PASS=testpass
